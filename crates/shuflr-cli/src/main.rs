#![cfg_attr(test, allow(clippy::unwrap_used, clippy::expect_used))]

fn main() -> std::process::ExitCode {
    eprintln!("shuflr: not implemented yet — see docs/design/002-revised-plan.md");
    std::process::ExitCode::from(1)
}
